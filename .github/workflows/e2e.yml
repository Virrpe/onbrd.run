name: E2E
on:
  workflow_dispatch:   # manual only; remove push/pull_request triggers
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Manual E2E only. Use locally or on demand."